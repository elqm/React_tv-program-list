import Shape from './Article.module.css';

function ArticleImage() {

    return (
        <div className={ Shape.shape }>
            
        </div>
    );
}

export default ArticleImage;